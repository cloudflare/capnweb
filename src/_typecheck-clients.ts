// Internal placeholder subpath. `capnweb typecheck gen` overwrites this with
// `__capnweb_wrap_<ClassName>` helpers consumed by the Vite plugin's
// client-side rewrite. Not part of capnweb's public API.

export {};
